# Prompts

The Atlas master prompt is **not published** in this public repository.

It is kept in a private local folder on the operator machine and is excluded from git (see `.gitignore`). Do not commit master-prompt files here.
